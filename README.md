## EECE385 Proj App: Security System

# Run the following command to activate virtual Python environment:

~ source venv/bin/activate ~ 

Useful command: pinout

use code editor nano <filename> if not comfortable with nvim <filename>

refer to gpiozero library documentation

git stuff:
dont bother pushing to remote, you can't authenticate yourselves as me without the key...
just do the following after develeopment is finished:

git status 
git add .  
git status
git commit -m "commit message -initials" //include your initials in the commit message

dont bother with: git push origin dev1

ssh pidev@pi3b.local 
